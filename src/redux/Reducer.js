let initialState = {
    videoId:''
}
function videoDetailReducer(state=initialState, action){
    switch(action.type){
        case 'GETVIDEOID':
            return {
                ...initialState,videoId:
            }
    }

}